# Распиновка: Arduino Nano V3

OLED **SSD1306 128×64** (I2C), кнопка, двухцветный светодиод, фотоприёмник на **A1**. Управление и логи также доступны через **USB / Serial** (`115200 8N1`) — прибор полностью работоспособен даже без OLED и без кнопки.

---

## SSD1306 128×64 (I2C)

Типичная маркировка выводов: **GND, VCC, SCL, SDA** (иногда порядок иной — сверяйте с шелкографией).

| Вывод модуля | Arduino Nano V3 | Примечание |
|--------------|-----------------|------------|
| **GND**      | **GND**         | Общая земля |
| **VCC**      | **5 V** *или* **3V3** | Часть модулей с преобразователем — **5 V**; чисто 3.3 В микросхема — только **3V3** (см. маркировку платы) |
| **SCL**      | **A5**          | Линия **SCL** шины `Wire` |
| **SDA**      | **A4**          | Линия **SDA** шины `Wire` |

Адрес I2C в коде по умолчанию **`0x3C`** (`cfg::kOledI2cAddress7bit` в `include/Config.h`). Если экран не отвечает — попробуйте **`0x3D`** или проверьте перемычку **ADDR** на плате.

Вывод **RST** на модуле, если есть, часто не тянут к МК — в `Config.h` задано **`kOledResetPin = -1`** (программный сброс по I2C).

**OLED не обязателен.** При отсутствии экрана в `include/Config.h` задайте **`kOledEnabled = false`** — прошивка не трогает I2C (A4/A5), зависаний на «висящей» шине не будет. Управление и логи — только Serial (см. ниже). Когда подключите модуль — **`kOledEnabled = true`** и перепрошейте.

---

## Кнопка → **D2**

| Контакт кнопки | Nano |
|----------------|------|
| Один вывод     | **D2** |
| Другой вывод   | **GND** |

`INPUT_PULLUP`: в покое **HIGH**, при нажатии **LOW**.

**Кнопка не обязательна.** Все жесты дублируются командами `m` / `c` / `r` / `h` из Serial.

---

## Двухцветный LED → **D4**, **D5**

Подразумевается **двухвыводный биколор** — два светодиода (красный/синий) встречно‑параллельно в одном корпусе. Ток в одном направлении — горит красный, в обратном — синий, без тока — оба погашены.

| Вывод LED | Подключение |
|-----------|-------------|
| Вывод **1** | **D4** |
| Вывод **2** | **D5** |

**Токоограничительный резистор** (например, **330 Ω**) ставится в разрыв **одного** из двух проводов — этого достаточно: ток в любую сторону всё равно проходит через тот же резистор. Двух резисторов не требуется.

| **D4** | **D5** | Результат |
|--------|--------|-----------|
| `HIGH` | `LOW`  | Один цвет (Red в прошивке) |
| `LOW`  | `HIGH` | Другой цвет (Blue в прошивке) |
| Одинаково (оба `LOW` или оба `HIGH`) | | Светодиод выключен |

Соответствие цветам задаётся в `BicolorLed::set`:

- `LedColor::Red`  → D4 = HIGH, D5 = LOW
- `LedColor::Blue` → D4 = LOW,  D5 = HIGH

Если по факту цвета визуально перепутаны — поменяйте местами провода D4/D5 (либо два `case` в `BicolorLed::set`). На корректность измерения это не влияет: знак `dirSign` рассчитывается автоматически по калибровке.

---

## Фотоприёмник → **A1**

Подойдёт любой из двух типов. Полярность (тёмно → высокий или низкий код АЦП) не критична: прошивка считает `cR`/`cB` и сама определяет, на какой «стороне» порога ждать каждое переключение.

### Вариант A — фототранзистор NPN

Подтяжка **коллектора** через **10 кΩ** на **5 V**, эмиттер на **GND**. Сигнал с коллектора → **A1**.

- Темно → коллектор подтянут к 5 В → высокий код АЦП (≈ 1023)
- Ярче на датчике → коллектор просел → низкий код АЦП

### Вариант B — фоторезистор (LDR)

LDR в делителе с подтяжкой **10 кΩ**. Включается одним из двух способов:

- LDR между **A1** и **5 V**, подтяжка **10 кΩ** от **A1** на **GND** → светло = **высокий** код.
- LDR между **A1** и **GND**, подтяжка **10 кΩ** от **A1** на **5 V** → светло = **низкий** код.

### Механика

Фотоприёмник должен быть **прижат к экрану монитора в светоизолирующей бленде** (присоска, резиновая муфта, отрезок термоусадки и т. п.). Посторонняя засветка топит оба уровня (R и B сближаются) и автоматически ловится проверкой пригодности как `dRB low`.

---

## USB / Serial → встроенный преобразователь Nano (CH340 / FT232)

Подключается одним **USB‑кабелем** к ПК. На плате этот канал жёстко занимает два цифровых пина:

| Сигнал | Пин Nano | Назначение |
|--------|----------|------------|
| TX | **D1** | UART TX (USB ↔ ПК) |
| RX | **D0** | UART RX (USB ↔ ПК) |

- Скорость: **115 200 8N1**.
- Команды (один символ, регистр не важен): `m` — меню, `c` — калибровка, `r` — замер, `h` — справка.
- Все экраны дублируются строками с префиксами `[MENU]`, `[CAL]`, `[CAL_RESULT]`, `[WARMUP]`, `[LAT]`, `[RESULT]`, `[ABORT]`.

> `D0` и `D1` физически выведены на гребёнку Nano, но **не используйте** их под GPIO — это разорвёт Serial‑консоль.

---

## Сводная таблица пинов

| Пин | Назначение | Можно не подключать? |
|-----|------------|----------------------|
| **A4** | OLED SDA (I2C) | да (тогда — только Serial) |
| **A5** | OLED SCL (I2C) | да |
| **D2** | Кнопка → GND | да (тогда — только Serial) |
| **D4** | Биколор LED (вывод 1) | **нет** — это стимул |
| **D5** | Биколор LED (вывод 2) | **нет** — это стимул |
| **A1** | Фотоприёмник | **нет** — это датчик |
| **D0**, **D1** | USB‑Serial (RX/TX) | **нет** — нужны для консоли |
| **5 V**, **GND** | Питание периферии | — |

### Минимальная рабочая конфигурация

Только то, без чего прибор измерять не может:

- **Биколор LED** на **D4/D5** (с одним токоограничивающим резистором)
- **Фотоприёмник** на **A1** (с подтяжкой 10 кΩ)
- **USB‑кабель** в ПК (питание + Serial)

OLED и кнопка — опциональны.

---

## Свободные пины для будущих модулей

Свободны (после вычета занятого выше): **D3**, **D6–D13**, **A0**, **A2–A3**, **A6–A7**.

- **D11**, **D12**, **D13** — это SPI (`MOSI / MISO / SCK`). Свободны под GPIO, но будут заняты при подключении SPI‑периферии.
- **A6**, **A7** на классическом Nano — **только АЦП**, без цифрового режима (нет порт‑регистра).

Свободного места достаточно для будущих расширений: дополнительный фотоприёмник, маркерный пин для логического анализатора (например, `D3` в кросс с фронтом смены цвета), внешний драйвер LED или потенциометр калибровки яркости.
