# Распиновка периферии на STM32F103C8 «Blue Pill»

Ниже: TFT **ST7789P3** 76×284, кнопка, двухпиновый биколорный светодиод, фотоприёмник. Логика **3.3 В** (как у STM32 и типичного модуля TFT).

**Важно:** по умолчанию **Serial** (USART2) в Arduino‑ядре STM32 часто выведен на **PA2 = TX**, **PA3 = RX** — эти пины **не используем** под дисплей, чтобы не ломать USB‑UART адаптер на плате/проводах.

---

# TFT ST7789P3 — вариант **76×284** (2.25")

Смещения в памяти контроллера (col/row) и `init(76, 284)` — см. комментарии в `include/Config.h` и ссылки на TFT_eSPI в README.

---

# TFT ST7789P3 (общая схема, 3.3 В)

Интерфейс — **4‑проводный SPI** (плюс DC, RST, при необходимости BL).

## Таблица проводов (аппаратный **SPI1**)

| Вывод TFT | Назначение     | Blue Pill (STM32F103) | Примечание |
|-----------|----------------|------------------------|------------|
| **GND**   | Общий минус    | **GND**                | |
| **VCC**   | Питание 3.3 В  | **3.3**                | Не подключать к 5 V |
| **SCL**   | SPI Clock      | **PA5** (SPI1_SCK)     | |
| **SDA**   | SPI MOSI       | **PA7** (SPI1_MOSI)    | |
| **CS**    | Chip Select    | **PA4**                | Должен совпадать с `kPinTftCs` |
| **DC**    | Data / Command | **PA1**                | |
| **RST**   | Сброс дисплея  | **PB0**                | |
| **BL**    | Подсветка      | **PB1** *или по модулю* | Если BL модуля на **GND** — см. `kBacklightHardwiredToGnd` в `Config.h`, провод MCU ↔ BL не вешать |

## Подсветка и «белый экран»

См. README и `Config.h`: **`kBacklightHardwiredToGnd`**, **`kDisplaySpiDataMode`**, **`kDisplayInvertColors`**, **`kDisplayRotation`**.

---

## Кнопка без фиксации → **PB12**

| Контакт кнопки | Blue Pill |
|----------------|-----------|
| Один вывод     | **PB12**  |
| Другой вывод   | **GND**   |

`INPUT_PULLUP`: в покое **HIGH**, при нажатии — **LOW**.

---

## Двухпиновый биколорный (красный / синий) светодиод → **PB6**, **PB7**

Корпус на **два вывода**: внутри обычно два кристалла **встречно‑параллельно**. Питается **между двумя пинами портов**, не «пин + GND».

| Вывод LED | Подключение |
|-----------|---------------|
| Вывод **1** | **PB6** (через ограничивающий резистор при необходимости) |
| Вывод **2** | **PB7** |

| **PB6** | **PB7** | Результат |
|---------|---------|-----------|
| `HIGH`  | `LOW`   | Один цвет |
| `LOW`   | `HIGH`  | Другой цвет |
| Одинаково | Одинаково | **Off** |

---

## Фотоприёмник → **PA0** (ADC1_IN0)

Прошивка: **`analogReadResolution(12)`**, шкала **0…4095**, опорное в вольтах — **`kAdcVrefVolts`** (по умолчанию **3.3 В**).

**Сборка под 3.3 В (замена схемы «10 кΩ → 5 V» с Nano):**

| Вывод        | Подключение |
|--------------|-------------|
| Эмиттер (типично) | **GND** |
| Коллектор    | узел **PA0**; от узла резистор **10 кΩ** на **3.3 V** |

При освещении напряжение на **PA0** **падает** (логика та же, что в README для Nano: темно → выше код АЦП, ярче → ниже). При переносе с платы на 5 В **обязательно** переделать делитель/подтяжку на **3.3 В** и общую землю с Blue Pill.

---

## Сводка занятых пинов Blue Pill (как в `Config.h`)

| Пин     | Назначение |
|---------|------------|
| **PA0** | Фотоприёмник (ADC) |
| **PA1** | TFT DC |
| **PA4** | TFT CS |
| **PA5** | TFT SCK (SPI1) |
| **PA7** | TFT MOSI (SPI1) |
| **PB0** | TFT RST |
| **PB1** | TFT BL (если не захардкожен на GND) |
| **PB6** | Биколор LED |
| **PB7** | Биколор LED |
| **PB12**| Кнопка → GND |

**Зарезервированы под UART2 / Serial (не трогать в разводке TFT):** **PA2** (TX), **PA3** (RX).

---

## Прошивка

Типично **ST‑Link** на разъём SWD (`SWDIO`, `SWCLK`, `GND`, `3.3`). В `platformio.ini`: `upload_protocol = stlink`. Для загрузки по UART потребуется **BOOT0** и отдельный протокол/настройка — не описано здесь.
